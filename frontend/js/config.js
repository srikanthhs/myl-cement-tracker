/* Application Configuration – Tier 1 / Presentation Layer */
const CONFIG = {
  API_BASE: '/api',   // Express backend; same origin in prod, proxy in dev
  TOKEN_KEY: 'myl_jwt',
  USER_KEY:  'myl_user',
};
